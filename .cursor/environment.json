{
  "name": "Lumen (Portfolio)",
  "install": "if [ -f package-lock.json ]; then npm ci; elif [ -f package.json ]; then npm install; else echo \"No package manifest yet — skipping dependency install\"; fi",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ],
  "ports": [
    {
      "name": "Next.js",
      "port": 3000
    }
  ]
}
